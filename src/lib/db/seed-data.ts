import { mockArtworks, mockExhibitions, mockOrders, mockTickets } from '@/lib/mock-data'

export const seedExhibitions = mockExhibitions
export const seedArtworks = mockArtworks
export const seedTickets = mockTickets
export const seedOrders = mockOrders
